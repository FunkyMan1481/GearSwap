<div class="container">
    <% @gears.each do |gear| %>
        <div class="card">
            <img src="<%= gear.image_url %>" alt="<%= gear.name %>">
            <div class="card-content">
                <h2><%= gear.nom %></h2>
                <p><%= gear.description %></p>
                <%= link_to 'Réserver', new_reservation_path(gear), class: 'btn' %>
            </div>
        </div>
    <% end %>
</div>
